.main.page-login.block-center
    .login_form
        h1.login_form__title= title 
        form.login_form__inputs 
            each field in fields
                | !{ field }
        .login_form__buttons
            | !{buttonEnter}
            | !{link}
